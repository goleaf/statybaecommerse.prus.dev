<?php

use Illuminate\Support\Facades\Route;
use Shopper\Core\Models\Product;

Route::get('/', function () {
    $products = Product::query()->publish()->limit(8)->get();

    return view('shop.index', compact('products'));
});
