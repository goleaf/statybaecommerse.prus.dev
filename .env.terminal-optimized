# Terminal Performance Optimization Configuration
# This file contains optimized settings to prevent terminal freezing

APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:ihYrJC/cnV+l5mElVUfTRpgZSQJfZD9uWjBbat6MtXo=
APP_DEBUG=false
APP_URL=https://statybaecommerse.prus.dev/

APP_LOCALE=lt
APP_FALLBACK_LOCALE=lt
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

# Optimized PHP CLI settings
PHP_CLI_SERVER_WORKERS=2
BCRYPT_ROUNDS=10

# Optimized logging
LOG_CHANNEL=single
LOG_LEVEL=error
LOG_DEPRECATIONS_CHANNEL=null

# Database optimization
DB_CONNECTION=sqlite
DB_FOREIGN_KEYS=true

# Session optimization
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Queue optimization
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync

# Cache optimization
CACHE_STORE=file
CACHE_PREFIX=laravel_cache

# Redis optimization (if needed)
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail optimization
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS settings
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Vite optimization
VITE_APP_NAME="${APP_NAME}"

# Disable debugbar and telescope for terminal performance
DEBUGBAR_ENABLED=false
TELESCOPE_ENABLED=false

# Shopper settings
SHOPPER_ENABLED_TWO_FACTOR=true
SHOPPER_PREFIX=cpanel

# Terminal-specific optimizations
TERMINAL_OPTIMIZED=true
DISABLE_DEBUG_TOOLS=true
OPTIMIZE_FOR_CLI=true
