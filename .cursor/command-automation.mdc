---
description: "Автоматизация команд для реалтайм мониторинга"
globs:
  - "**/*.php"
  - "**/*.blade.php"
alwaysApply: true
---

# АВТОМАТИЗАЦИЯ КОМАНД

## АВТОМАТИЧЕСКИЙ ЗАПУСК КОМАНД

При каждом изменении файла автоматически выполняй:

### 1. ПРОВЕРКА СИНТАКСИСА
```bash
php -l {changedFile}
```

### 2. ИСПРАВЛЕНИЕ СТИЛЯ
```bash
vendor/bin/pint {changedFile} --test || vendor/bin/pint {changedFile}
```

### 3. СТАТИЧЕСКИЙ АНАЛИЗ
```bash
vendor/bin/phpstan analyse {changedFile} -c phpstan.neon --memory-limit=1G --no-progress
```

### 4. АВТОРЕФАКТОРИНГ
```bash
vendor/bin/rector process {changedFile} --ansi --no-progress-bar
```

### 5. ОБНОВЛЕНИЕ КЭШЕЙ
```bash
php artisan config:clear && php artisan route:clear
php artisan config:cache && php artisan route:cache
```

### 6. ЗАПУСК ТЕСТОВ
```bash
php artisan test --filter={TestName} --stop-on-failure
```

## УСЛОВИЯ ЗАПУСКА

### Всегда запускать:
- При сохранении PHP файлов
- При изменении Blade шаблонов
- При изменении конфигурационных файлов

### Запускать с задержкой:
- При наборе текста (каждые 2-3 секунды)
- При вставке кода (через 1 секунду)
- При автодополнении (через 500мс)

## ОБРАБОТКА ОШИБОК

### При ошибке синтаксиса:
1. Показать ошибку
2. Предложить исправление
3. Применить исправление автоматически
4. Повторить проверку

### При ошибке стиля:
1. Применить исправления Pint
2. Показать изменения
3. Сохранить файл

### При ошибке анализа:
1. Показать предупреждения
2. Предложить исправления
3. Применить автоматические исправления

### При ошибке рефакторинга:
1. Показать изменения
2. Подтвердить применение
3. Сохранить файл

## ЛОГИРОВАНИЕ

```
🔄 Запуск: {команда} для {файл}
✅ Успешно: {результат}
❌ Ошибка: {описание}
🔧 Исправлено: {количество} ошибок
```

## ПРОИЗВОДИТЕЛЬНОСТЬ

- Используй параллельные процессы
- Кэшируй результаты
- Обрабатывай только изменённые файлы
- Пропускай vendor/ и node_modules/

## ОСТАНОВКА

Цикл останавливается при:
- Отсутствии ошибок
- Достижении лимита попыток
- Критической ошибке
- Ручной остановке пользователя