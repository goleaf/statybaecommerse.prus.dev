---
description: "Laravel 11 + Filament v4 — РЕАЛТАЙМ автоанализ, автотесты и автоисправления при каждом изменении"
globs:
  - "**/*.php"
  - "**/*.blade.php"
  - "composer.json"
  - "phpstan.neon"
  - "rector.php"
  - "app/**/*.php"
  - "database/**/*.php"
  - "config/**/*.php"
  - "routes/**/*.php"
  - "tests/**/*.php"
alwaysApply: true
---

# РЕАЛТАЙМ АВТОФИКС СИСТЕМА

При КАЖДОМ изменении файлов (сохранение, вставка, удаление) автоматически выполняй:

## 1. МГНОВЕННАЯ ПРОВЕРКА СИНТАКСИСА
```bash
# Проверка изменённого файла
php -l {changedFile}

# Если ошибка - немедленно исправь и повтори
```

## 2. АВТОМАТИЧЕСКОЕ ИСПРАВЛЕНИЕ СТИЛЯ
```bash
# Исправление стиля кода
vendor/bin/pint {changedFile} --test || vendor/bin/pint {changedFile}
```

## 3. СТАТИЧЕСКИЙ АНАЛИЗ
```bash
# Анализ изменённого файла
vendor/bin/phpstan analyse {changedFile} -c phpstan.neon --memory-limit=1G --no-progress
```

## 4. АВТОРЕФАКТОРИНГ
```bash
# Применение правил рефакторинга
vendor/bin/rector process {changedFile} --ansi --no-progress-bar
```

## 5. ПРОВЕРКА BLADE ШАБЛОНОВ
```bash
# Если изменён Blade файл
php artisan view:clear && php artisan view:cache
```

## 6. БЫСТРЫЕ ТЕСТЫ
```bash
# Запуск тестов для изменённых компонентов
php artisan test --filter={TestName} --stop-on-failure
```

## 7. КЭШИРОВАНИЕ
```bash
# Обновление кэшей
php artisan config:clear && php artisan route:clear
php artisan config:cache && php artisan route:cache
```

## АВТОМАТИЧЕСКИЕ ИСПРАВЛЕНИЯ

При обнаружении ошибок автоматически применяй:

### PHP Синтаксис:
- Исправление отсутствующих точек с запятой
- Добавление отсутствующих скобок
- Исправление неправильных кавычек
- Добавление отсутствующих use statements

### Filament v4:
- `use Filament\Schemas\Schema;` → удалить
- `public static function form(Schema $schema): Schema` → `public static function form(Form $form): Form`
- `public static function table(Table $table): Table` → исправить сигнатуру
- `$schema` → `$form` в теле метода form()
- `protected static $navigationIcon` → добавить docblock `/** @var string|\BackedEnum|null */`

### Laravel:
- Добавление отсутствующих импортов
- Исправление типов возвращаемых значений
- Добавление declare(strict_types=1)
- Исправление namespace declarations

### Blade:
- Исправление синтаксиса @if, @foreach, @php
- Добавление отсутствующих @end директив
- Исправление {{ }} и {!! !!} синтаксиса

## ПРИОРИТЕТЫ ИСПРАВЛЕНИЙ

1. **КРИТИЧЕСКИЕ** - Синтаксические ошибки (останавливают выполнение)
2. **ВЫСОКИЕ** - Ошибки типов и отсутствующие импорты
3. **СРЕДНИЕ** - Стиль кода и форматирование
4. **НИЗКИЕ** - Предупреждения и рекомендации

## ЛОГИРОВАНИЕ

После каждого цикла исправлений выводи:
```
✅ Исправлено: {количество} ошибок в {файл}
🔧 Применено: {список исправлений}
⚠️  Предупреждения: {количество} (не критично)
```

## НЕПРЕРЫВНЫЙ МОНИТОРИНГ

Система должна работать в фоновом режиме и реагировать на:
- Сохранение файлов (Ctrl+S)
- Вставку кода (Ctrl+V)
- Автодополнение
- Изменение импортов
- Рефакторинг

## ОСТАНОВКА ЦИКЛА

Цикл исправлений останавливается когда:
- Все ошибки исправлены
- Достигнуто максимальное количество попыток (5)
- Обнаружена критическая ошибка, требующая ручного вмешательства

## ПРОИЗВОДИТЕЛЬНОСТЬ

- Используй параллельные процессы где возможно
- Кэшируй результаты анализа
- Обрабатывай только изменённые файлы
- Пропускай vendor/ и node_modules/