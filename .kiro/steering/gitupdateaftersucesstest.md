---
inclusion: manual
---

# Git updates after work (optional)

Use this only when the user explicitly asks to commit and/or push.

- Ensure relevant tests pass (prefer a narrow filter first, then full suite if requested).
- Stage only the files changed for the current task.
- Commit message: short summary + key scope (avoid dumping every filename into the subject).
- Push only when explicitly requested.
