---
inclusion: manual
---

# PHP rules (optional)

- Use `declare(strict_types=1);` at the top of PHP files.
- Prefer `match` over `switch` when appropriate.
- Use constructor property promotion; don’t add empty constructors.
- Add explicit parameter and return types.
