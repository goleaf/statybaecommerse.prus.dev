## 1. Implementation
- [ ] 1.1 Add product import option UI for sync keys (repeatable) and CSV key mapping preview.
- [ ] 1.2 Implement sync key matching logic in `ProductImporter` for upsert behavior.
- [ ] 1.3 Update sync mode copy to reflect upsert-only behavior.
- [ ] 1.4 Add tests for sync key matching (single key, priority fallback, ambiguous match).

## 2. Verification
- [ ] 2.1 Run targeted tests (Product importer tests).
- [ ] 2.2 Validate OpenSpec change.
