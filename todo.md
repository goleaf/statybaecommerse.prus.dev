# Priority Tasks — 2025-10-22

1. Align RecommendationBlock resource copy with structured admin translation keys and shared option helpers.
2. Refresh RecommendationBlock list tabs for translated labels and accurate counts.
3. Normalise UnitEnum imports plus navigation icon docblocks across touched Filament resources.
4. Add Lithuanian and English admin translations for recommendation blocks.
1. Audit enum metadata usage counts within Enum Value resource and supporting data sources.
2. Implement duplication reset logic and unused enum cleanup routines with safety checks.
3. Expose usage metadata in factories, filters, and translations tied to enum values.
4. Repair Filament admin imports affecting package discovery and align Variant Analytics resource navigation icon typing.
5. Execute relevant tests and static checks to validate enum and Filament adjustments.
