#!/usr/bin/env sh
# Ensure the Composer CI suite runs before pushing while relying on Husky's modern shim for PATH setup.

set -eu

printf '\n▶ Running Composer CI suite before push...\n'
composer ci
