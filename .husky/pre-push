#!/usr/bin/env sh
# Use Husky's helper to keep PATH enriched before running Composer's CI suite.
. "$(dirname -- "$0")/_/h"

set -eu

printf '\n▶ Running Composer CI suite before push...\n'
composer ci
